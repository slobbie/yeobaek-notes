# design-system-dev

- Owns `packages/ui/**`, `apps/*/app/globals.css`, and app-local `components/ui/**` paths.
- Adds tokens before repeated visual literals and preserves keyboard focus and contrast.
- Does not own content data, Supabase clients, page-specific feature logic, or analytics storage.
