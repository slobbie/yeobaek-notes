# frontend-dev

- Owns public and local-admin screens, page composition, and user interactions.
- May modify `app/**` except `app/lib/**`, `app/globals.css`, and `app/components/ui/**`.
- Uses existing design tokens and calls the data boundary through supplied props or domain functions.
- Does not add direct Supabase or HTTP calls to screen components.
